# 🎵 Weezer — Social Network

A full-stack social network project built for learning and practice.  
Runs with Docker Compose and simulates a real production-like setup.

---

## 🚀 Quick Start

Start everything:

```bash
docker compose up -d
Services that will run:

backend

frontend

mysql

localstack

socket service (io)

🧩 Run a Single Service
bash
Copy code
docker compose up backend
docker compose up frontend
docker compose up mysql
docker compose up localstack
⚙️ Configuration
All ports and environment variables are defined in docker-compose.yml

Each service has its own Dockerfile

Internal communication is done via Docker service names
(e.g. mysql, backend)

🛠 Stack
TypeScript · React · Node.js · MySQL · Docker · Stripe · OAuth · Socket.IO

Personal learning project focused on clean architecture
and real-world backend & frontend integration.
